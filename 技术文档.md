# vue_vite_demo 前端项目技术文档

> 版本：基于仓库当前快照（package.json `version: 0.0.0`）
> 分析方式：逐文件静态阅读 `src/` 全部 57 个源文件（23 个 `.ts` + 30 个 `.vue` + 3 个 `.json` 地图资源）+ 构建配置 + 入口 HTML
> 定位：**一个面向生产的 AI 对话产品前端 SPA**，自带管理后台（用户 / 模型 / 用量 / 地域热力 / 建议语 / 系统设置）与中英双语、多主题、移动端适配能力。

---

## 目录

1. [项目概览](#1-项目概览)
2. [技术栈](#2-技术栈)
3. [目录结构](#3-目录结构)
4. [架构总览与数据流](#4-架构总览与数据流)
5. [核心机制详解](#5-核心机制详解)
6. [模块详解：基础设施层](#6-模块详解基础设施层)
7. [模块详解：组件层](#7-模块详解组件层)
8. [关键业务流程](#8-关键业务流程)
9. [前端调用的后端接口契约汇总](#9-前端调用的后端接口契约汇总)
10. [设计亮点与可改进点](#10-设计亮点与可改进点)

---

## 1. 项目概览

这是一个 **Vue 3 + TypeScript + Vite** 单页应用，本质是一个 **AI 聊天产品** 的前端：

- 普通用户：登录 / 注册 → 多会话聊天（流式输出、图片输入、联网搜索、Markdown 渲染、复制、重生成、置顶、主题 / 语言切换、个人资料编辑、头像裁剪）。
- 管理员：`/admin` 后台 —— 数据总览仪表盘、用量统计、用户 / 模型 / 建议语管理、系统设置（键值对 + 搜索 provider 配置）、**中国地图省级热力 + 市级下钻** 的地域分布可视化。
- 后端解耦：所有业务请求走 `/api`，开发时由 Vite 代理到 `http://localhost:8000`，**API Key 由后端持有**，前端不接触密钥；会话、消息、用户、用量等数据全部由后端落库。

辅助文档（仓库内已有，非本代码生成）：`README.md`（模板默认）、`PROJECT_SUMMARY.md`、`QUICK_START.md`、`QUICK_REFERENCE.txt`、`START_HERE.md`、`README_CHAT.md`、`DELIVERY_CHECKLIST.md`。

---

## 2. 技术栈

| 分类 | 选型 | 版本 | 用途 |
|---|---|---|---|
| 框架 | Vue 3 | ^3.5.13 | `<script setup>` + Composition API |
| 构建 | Vite | ^6.0.1 | 开发服务器、构建、HMR |
| 语言 | TypeScript | ~5.6.3 | 类型安全（`vue-tsc` 做类型检查） |
| 状态 | Pinia | ^4.0.2 | 全局状态（auth / chat 两个 store） |
| 路由 | vue-router | ^4.6.4 | 路由表 + 导航守卫 |
| 国际化 | vue-i18n | ^11.4.8 | 中英双语（zh-CN / en-US） |
| 图表 | ECharts | ^6.1.0 | 仪表盘、用量、地图可视化 |
| Markdown | marked + DOMPurify | ^18 / ^3.4.13 | 助手消息富文本渲染 + XSS 防护 |
| 图标 | @iconify/vue | ^5.0.1 | 统一图标 |
| 粒子背景 | @tsparticles/vue3 + slim | ^4.3.2 | 科技感粒子 |
| 代码质量 | ESLint 9 + Vue TS 配置 | ^9.14.0 | 静态检查（`npm run lint`） |

**脚本（`package.json`）**

| 命令 | 含义 |
|---|---|
| `npm run dev` | 启动 Vite 开发服务器 |
| `npm run build` | `run-p type-check "build-only {@}"` —— 先类型检查再构建 |
| `npm run build-only` | `vite build`（仅构建） |
| `npm run preview` | 预览构建产物 |
| `npm run type-check` | `vue-tsc --build`（类型检查） |
| `npm run lint` | `eslint . --fix` |

**`vite.config.ts` 要点**：`vue()` + `vite-plugin-vue-devtools` 插件；`resolve.alias['@'] → ./src`；`server.proxy['/api'] → http://localhost:8000`（`changeOrigin: true`）—— 这是前后端联调的关键桥梁。

---

## 3. 目录结构

```
vue_vite_demo/
├── index.html                 # SPA 入口 HTML（挂载 #app，加载字体）
├── vite.config.ts            # Vite 配置（别名、代理、devtools）
├── tsconfig*.json            # TS 配置（app / node）
├── eslint.config.js          # ESLint 配置
├── env.d.ts                  # Vite 客户端类型引用
├── public/favicon.ico
├── dist/                     # 构建产物（47 文件）
└── src/
    ├── main.ts               # 应用入口：装配 pinia/router/i18n/particles + 401 处理
    ├── App.vue               # 根组件：背景 + RouterView + Toast
    ├── router/index.ts       # 路由表 + 导航守卫
    ├── stores/               # Pinia 状态层
    │   ├── authStore.ts      # 登录态 / 角色
    │   └── chatStore.ts      # 会话 / 消息 / 主题 / 模型
    ├── services/             # API 服务层（统一封装 fetch）
    │   ├── apiService.ts     # 聊天 / 会话 / SSE 流式
    │   ├── authService.ts    # 登录 / 注册 / 资料
    │   ├── adminService.ts   # 后台全部接口
    │   ├── token.ts          # JWT 存取
    │   └── unauthorized.ts   # 401 回调注册表
    ├── types/                # 数据契约（前后端类型）
    │   ├── chat.ts
    │   └── admin.ts
    ├── config/models.ts      # 模型 & 提供方字典
    ├── locales/              # i18n
    │   ├── index.ts          # i18n 实例 + setLocale/getLocale
    │   ├── zh-CN.ts          # 中文词条（297 行）
    │   └── en-US.ts          # 英文词条（302 行）
    ├── styles/               # 全局样式 & 主题
    │   ├── global.css        # CSS 变量、字体、Markdown、液态玻璃、动画、响应式
    │   └── themes.ts         # 8 套主题配置 + applyTheme
    ├── composables/          # 复用逻辑
    │   ├── useDevice.ts      # 设备类型（移动/桌面）探测
    │   ├── useSafeArea.ts    # 安全区域（刘海屏）适配
    │   └── useToast.ts       # 全局 Toast 队列
    ├── utils/                # 纯函数工具
    │   ├── avatar.ts         # 默认头像 / 圆形裁剪（Canvas）
    │   ├── format.ts         # 时间格式化
    │   ├── image.ts          # 图片读取 / 头像裁剪导出
    │   └── provinceHeat.ts   # 省份热度计算
    ├── assets/maps/          # 地图资源
    │   ├── china.json        # 全国省级 GeoJSON（582 KB）
    │   ├── city-coords.json  # 城市经纬度字典（12.8 KB）
    │   └── region-data.json  # 省/市/区三级联动数据（137 KB）
    └── components/           # 视图层（30 个组件）
        ├── chat/            # 11 个：聊天主场景
        ├── auth/            # 1 个：登录注册
        ├── admin/           # 11 个：管理后台
        └── common/          # 6 个：通用基础组件
```

---

## 4. 架构总览与数据流

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器 (SPA)                            │
│                                                               │
│  components/ (chat/auth/admin/common)                         │
│        │ 调用（读写状态 / 触发动作）                            │
│        ▼                                                      │
│  stores/ (Pinia)  authStore ────┐    chatStore ────┐          │
│        │ 调用（数据获取）          │                │          │
│        ▼                         ▼                ▼          │
│  services/  authService   apiService        adminService     │
│        │ 统一 fetch + Bearer Token                            │
│        │ 401 → notifyUnauthorized()                          │
│        ▼                                                      │
│  /api/*  (fetch)  ──────►  Vite dev proxy  ──────►  :8000    │
└─────────────────────────────────────────────────────────────┘
        ▲                                  ▲
        │  SSE 流式分片 (text/event-stream) │  JSON
        └──────────────────────────────────┘
```

**分层职责**

- **视图层（components）**：只负责 UI 与交互，所有数据/动作通过 store 与 service 获取，自身不直连后端（少数组件如 `AdminSettingsSearch` 内联 `fetch` 测试 searxng 连通性属例外）。
- **状态层（stores）**：Pinia 两个 store 是业务中枢 —— `authStore` 管登录态与角色，`chatStore` 管会话 / 消息 / 主题 / 模型，并封装乐观更新与流式调用。
- **服务层（services）**：统一 `fetch` 封装，集中处理 Bearer Token、401、错误解析；`apiService` 额外实现 SSE 流式与超时。
- **类型层（types）**：`chat.ts` / `admin.ts` 是前后端数据契约，所有 store / service / 组件共享。
- **配置 / i18n / 样式 / composables / utils**：横切关注点，被各层复用。

**鉴权链**：`services`（401）→ `notifyUnauthorized()` → `main.ts` 注册的 handler → `authStore.reset()` + Toast + `router.replace('/login')`。

---

## 5. 核心机制详解

### 5.1 鉴权与路由守卫（`router/index.ts` + `authStore` + `unauthorized.ts`）

- **路由元信息（meta）**：`/chat` 标记 `requiresAuth: true`；`/admin` 标记 `requiresAuth` + `requiresAdmin: true`；`/` 根据 `auth.isLoggedIn` 重定向到 `/chat` 或 `/login`。
- **`beforeEach` 守卫**（`router/index.ts:94`）：
  1. `await auth.init()` —— 若本地有 token 则拉取 `/auth/me`，否则静默。
  2. `requiresAuth` 且未登录 → 跳 `/login` 并带 `?redirect=当前路径`。
  3. `requiresAdmin` 且非管理员 → 跳 `/chat`。
  4. 已登录访问 `/login` 或 `/register` → 跳 `/chat`。
- **401 统一处理**：任何 service 收到 401 → `clearToken()` + `notifyUnauthorized()`；`main.ts` 注册的 handler 重置 auth、弹错误 Toast、跳登录页（保留 `redirect`）。
- **Token 存储**：`token.ts` 用 `localStorage['chatToken']`，仅存 JWT 字符串，刷新后可凭 token 恢复会话（`auth.init` 的 `initPromise` 做去重，避免重复拉取）。

### 5.2 主题系统（`themes.ts` + `global.css` + `chatStore`）

- 8 套主题：`dark`（默认）、`light`、`neon`、`ocean`、`midnight`、`amber`、`mint`、`sand`。
- `themes.ts` 定义每套主题的 14 个颜色变量（primary / secondary / background / surface / text / textSecondary / accent / border / glass / overlay / grid / glow / glassEdge / glassSheen）。
- `applyTheme(theme)` 把配置写入 `document.documentElement` 的 CSS 变量（`--color-*`）。
- `global.css` 的 `:root` 提供默认值，并定义字体（Orbitron / Rajdhani / Share Tech Mono）、玻璃拟态、网格 + 极光背景、滚动条、Markdown 样式、全套动画与 `@media (max-width:768px)` 响应式断点。
- **持久化**：`chatStore.setTheme/loadTheme` 用 `localStorage['chatTheme']`；`App.vue` 挂载时 `loadTheme()` + `applyTheme()`；`SciFiBackground` 与 `AdminMap` 读取 `chatStore.currentTheme` 实时联动（地图在主题切换时重建实例）。

### 5.3 国际化（i18n）

- `locales/index.ts`：`createI18n({ legacy:false, globalInjection:true })`；初始语言读 `localStorage['chatLocale']`，缺省 `zh-CN`，`fallbackLocale: 'zh-CN'`；`setLocale` 同步写入 localStorage 并改 `<html lang>`。
- 词条命名空间（zh-CN 297 行 / en-US 302 行）：`app`、`common`、`input`、`chat`、`model`、`theme`、`api`、`confirm`、`auth`、`profile`、`console`、`datePicker`。
- 组件内用 `useI18n()` 或全局 `$t`，service 层用 `i18n.global.t(...)`（如错误信息、超时报错）。

### 5.4 设备适配与移动端

- `useDevice.ts`：基于 `matchMedia('(pointer: coarse)')` 等探测 `isMobile / isDesktop / isTouch / isHoverDevice`，并在 `<html>` 切换 `device-mobile` / `device-desktop` 类（CSS 据此切换侧边栏 / 抽屉等布局）。返回 `readonly` 避免误改。
- `useSafeArea.ts`：通过 `env(safe-area-inset-top/bottom)` 测量刘海屏安全区，rAF 节流写入 `--safe-top` / `--safe-bottom`；监听 `orientationchange` / `resize` 重算。`App.vue` 在应用根调用一次。

### 5.5 全局反馈（Toast）

- `useToast.ts`：模块级 `reactive` 数组 `toasts` + `showToast(msg, type)`（3.2s 自动消失）；`AppToast.vue` 用 `<transition-group>` 渲染，被全应用复用。

### 5.6 SSE 流式与三层超时（`apiService.chatStream`）

- `POST /api/chat`，请求体：`{ model, messages, web_search, session_id, ... }`，后端透传 OpenAI 兼容格式 SSE。
- 前端手动解析：`parseStreamLine` 处理 `data: {...}` 行，`[DONE]` 结束，`error` 视为错误事件，`meta.assistant_id` 透传用量信息（`ChatStreamMeta`）。
- **三层超时**（防死锁）：
  - `FIRST_CHUNK_TIMEOUT = 30s`：连接后无首包则 abort；
  - `IDLE_TIMEOUT = 60s`：收到分片后重置，空闲超时则 abort；
  - `MAX_TOTAL_TIMEOUT = 5min`：兜底总超时。
- **取消**：`AbortController` 存于 `Map<requestId, controller>`，`apiService.abort(requestId)` 供 `chatStore.abortCurrentRequest` / 删除会话时调用。

---

## 6. 模块详解：基础设施层

### 6.1 入口（`main.ts` / `App.vue` / `index.html`）

- **`main.ts`**：`createApp(App)` → `use(pinia)` / `use(router)` / `use(i18n)` / `use(Particles, { init: loadSlim })`；注册 `setUnauthorizedHandler`（401 → 重置 auth + Toast + 跳登录带 `redirect`）；`mount('#app')`。
- **`App.vue`**：模板三层 `<SciFiBackground/> <RouterView/> <AppToast/>`；`onMounted`：`auth.init()`、`chat.loadTheme()`、`applyTheme(chat.currentTheme)`；调用 `useSafeArea()`。
- **`index.html`**：`<div id="app">` + `<script type=module src=/src/main.ts>`；预连接 Google Fonts（Orbitron / Rajdhani / Share Tech Mono），`viewport` 含 `viewport-fit=cover` 支持安全区，`theme-color #070a1a`。

### 6.2 路由（`router/index.ts`）

| 路径 | 组件 | meta |
|---|---|---|
| `/` | 重定向（按登录态） | — |
| `/login` | `AuthPage` (mode=login) | — |
| `/register` | `AuthPage` (mode=register) | — |
| `/chat` | `ChatWindow` | requiresAuth |
| `/admin` | `AdminConsole` | requiresAuth, requiresAdmin |
| `/admin/overview` | `AdminOverview` | 子路由 |
| `/admin/models` | `ModelManage` | 子路由 |
| `/admin/users` | `AdminUsers` | 子路由 |
| `/admin/map` | `AdminMap` | 子路由 |
| `/admin/usage` | `AdminUsage` | 子路由 |
| `/admin/suggestions` | `AdminSuggestions` | 子路由 |
| `/admin/settings/base` | `AdminSettingsBase` | 子路由（settings 布局内） |
| `/admin/settings/search` | `AdminSettingsSearch` | 子路由 |
| `/:pathMatch(.*)*` | 重定向 `/` | — |

全部组件使用 `() => import(...)` 懒加载。守卫逻辑见 5.1。

### 6.3 状态层（`stores`）

#### `authStore.ts`
- 状态：`user: AuthUser | null`。
- 计算：`isLoggedIn = !!user`；`isAdmin = user?.role === 'admin'`。
- 方法：`init()`（凭 token 拉 `/auth/me`，`initPromise` 去重）、`login/register`（拿 token → `setToken` → `fetchMe`）、`logout/reset`、`updateProfile`、`checkUsername`。

#### `chatStore.ts`
- 状态：`sessions`、`messageCache`（按 sessionId 缓存消息数组）、`hasMoreMap`、`loadingOlder`、`currentSessionId`、`currentTheme`、`availableModels`（默认 `MODEL_LIST`，`loadModels` 成功后被后端覆盖）、`requestIds`（当前请求的 requestId）。
- 计算：`currentSession`、`sortedSessions`（排序由后端决定）、`messages`、`currentModel`（取会话模型或 `DEFAULT_MODEL = 'tencent/Hunyuan-MT-7B'`）、`currentModelInfo`、`availableThemes`。
- 会话 / 消息：
  - `init()`：先 `auth.init()`，拉会话列表；若无会话则建一个；
  - `loadMessages`（首次加载，带 `has_more`）、`loadOlderMessages`（向上分页，`before_id`，滚动哨兵触发）；
  - `addMessage / updateMessage / deleteMessage / clearMessages`（乐观更新，最终以服务端 `applyList` 同步）；
  - `togglePin / setModel / setWebSearch / updateSessionTitle`（PATCH 会话并乐观回退）。
- **`sendMessageStream`**（核心）：
  1. 确保有当前会话；
  2. 乐观插入 user 消息 + 空 assistant 消息（若开启联网搜索则置 `isSearching`）；
  3. `buildHistory` 取最近 `MAX_HISTORY=20` 条、排除占位助手消息；
  4. 调 `apiService.chatStream`，`onChunk` 累加内容；收到首包时关闭搜索态并补「搜索耗时」；
  5. 异常（非 AbortError）写入错误文本；`finally` 解除 loading 并 `refreshSessions()`。
- `abortCurrentRequest`、`setTheme/loadTheme`（localStorage）、`reset`。

### 6.4 服务层（`services`）

| 文件 | 职责 | API_BASE |
|---|---|---|
| `apiService.ts` | 聊天 SSE、会话 CRUD、消息分页、模型 / 建议语获取 | `/api` |
| `authService.ts` | 登录 / 注册 / 用户名校验 / 资料读取与更新 | `/api` |
| `adminService.ts` | 后台全部接口（统计 / 用户 / 模型 / 用量 / 地域 / 设置 / 建议） | `/api/admin` |
| `token.ts` | `localStorage['chatToken']` 读写删 | — |
| `unauthorized.ts` | 401 回调注册表（`setUnauthorizedHandler` / `notifyUnauthorized`） | — |

- **统一模式**：`authHeaders()` 自动注入 `Authorization: Bearer <token>`；`handleJson` / `parseError` 解析 `{detail}` 错误；401 → `clearToken()` + `notifyUnauthorized()` + throw。
- `adminService.request<T>()` 是泛型封装；`apiService` 额外 `mapSession` / `mapSessionList` 把后端 snake_case 映射为前端 camelCase（`web_search` → `webSearch` 等）。
- 接口清单见第 9 节。

### 6.5 类型层（`types`）

- **`chat.ts`**：`MessageRole`、`ThemeType`（8 主题）、`ApiProvider`、`Message`（含 `loading/images/isSearching`）、`SendPayload`、`ChatSession`、`SessionPatch`、`ChatStreamMeta`（用量）、`ThemeConfig`、`ApiConfig`、`ModelInfo`（`free/vision`）。
- **`admin.ts`**（最庞大，约 240 行）：`UserRole`、`Gender`、`AuthUser`（含 `username_changes_left`）、`ProfileUpdatePayload`、`AdminModel`、`AdminStats`、`AdminUser`、`ModelPayload`、`UserUpdatePayload`、`UsageByUser/Model/Daily/DistItem`、`AdminUsage`、`SettingItem`、`SuggestionItem/Payload`、`RegionTopUser`、`RegionStat`、`HeatPeriod`（`day/week/month/year`）、`SearchProvider`、`ProvinceMetric`、`RegionStatsData`、`Overview*`（概览各类图表数据）、`AdminOverview`。

### 6.6 配置（`config/models.ts`）

- `DEFAULT_PROVIDERS`：20 个模型提供方字典（openai / anthropic / qwen / deepseek / tencent / ollama …），可被后台 `model_providers` 设置覆盖。
- `MODEL_LIST`：约 60 个对话模型（硅基流动实测），每条含 `id / name / free / vision`；`free` 标记依据官方价格页。

### 6.7 样式（`styles`）

- `global.css`：`:root` 主题变量 + 字体 + 滚动条 + 全屏网格/极光背景（`body::before/::after` 动画）+ `.markdown-body` 富文本 + `.liquid-glass` 液态玻璃 + 全套 `@keyframes` + `@media (max-width:768px)`。
- `themes.ts`：8 套主题 `ThemeConfig` + `applyTheme`（写入 14 个 `--color-*` / 玻璃变量）。

### 6.8 Composables

- `useDevice`：`matchMedia` 探测设备类型，`readonly` 暴露，切换 `<html>` class。
- `useSafeArea`：rAF 节流测量安全区 → `--safe-top/--safe-bottom`；监听 `orientationchange/resize`。
- `useToast`：模块级 `reactive toasts` + `showToast`（3.2s 自动消失）。

### 6.9 Utils（纯函数）

- `avatar.ts`：SVG 默认头像、`avatarSrc`（空值兜底）、`roundAvatarDataUrl`（Canvas 圆形裁剪 + 可选描边环 + `Map` 缓存）。
- `format.ts`：`formatDateTime(ms)`（YYYY-MM-DD HH:mm:ss，空值返回 `-`）。
- `image.ts`：`readFileAsDataUrl`（校验图片类型、≤8MB）、`cropToAvatarDataUrl`（256px 圆裁，导出 webp/png 兜底）。
- `provinceHeat.ts`：`HEAT_PERIODS`（4 周期）、`PROVINCE_HEAT_WEIGHTS`（新增 25% / 人均对话 40% / 对话 20% / Token 15%）、`computeProvinceHeat`（Log(1+x) 压缩 → 周期内按省归一化 → 加权 0~100），供 `AdminMap` 配色。

### 6.10 地图资源（`assets/maps`）

- `china.json`：全国省级 GeoJSON（582 KB），`AdminMap` 注册为 ECharts 地图。
- `city-coords.json`：城市经纬度字典，作为坐标解析的回退之一。
- `region-data.json`：省/市/区三级数据，供 `RegionSelect` 三级联动。

---

## 7. 模块详解：组件层

### 7.1 `components/chat/`（11 个）

| 组件 | 职责 | 关键依赖 |
|---|---|---|
| `ChatWindow.vue` | 聊天主容器：会话列表 + 消息区 + 输入区 + 设置按钮聚合 | chatStore / authStore / useDevice / useToast；子组件 MessageList / InputBox / ThemeSwitcher / ModelSelector / LanguageSwitcher / ConfirmModal / UserMenu / UserProfileDialog |
| `MessageList.vue` | 消息列表：向上懒加载、自动滚底、空态建议 | chatStore / apiService.fetchSuggestions；`IntersectionObserver` 顶哨兵 + 滚动位置补偿 |
| `MessageItem.vue` | 单条消息：用户纯文本 / 助手 Markdown + 流式光标 + 复制 | marked + DOMPurify（自定义 code 渲染器 + 复制按钮，`a` 加 `target=_blank rel=noopener`） |
| `InputBox.vue` | 输入框：文本 + 图片（≤4 张/4MB）+ 联网搜索开关 | chatStore；`FileReader` 转 DataURL；Enter 发送（排除 Shift / 中文合成）；非 vision 模型自动清图 |
| `ModelSelector.vue` | 模型下拉 | chatStore（currentModel / availableModels / setModel） |
| `LanguageSwitcher.vue` | 语言切换（zh-CN / en-US） | locales 的 getLocale / setLocale |
| `ThemeSwitcher.vue` | 主题切换 | chatStore + applyTheme |
| `SciFiBackground.vue` | 科技感粒子背景 | chatStore.currentTheme 决定调色板（links/grab 交互） |
| `UserMenu.vue` | 移动端用户菜单 Sheet（Teleport，Pointer 拖拽关闭，阈值 110 / 速度 0.55） | authStore / chatStore / LanguageSwitcher |
| `UserProfileDialog.vue` | 资料编辑弹窗（Teleport，用户名异步校验 450ms 防抖、改名次数锁定、Esc 关闭、移动端拖拽） | authStore / authService / utils.image / RegionSelect / DatePicker / AvatarCropDialog |
| `AvatarCropDialog.vue` | 头像裁剪（圆形舞台 + 缩放 slider + Pointer 平移） | utils.image.cropToAvatarDataUrl |

**chat 模块小结**：`ChatWindow` 为聚合根，核心交互（流式发送、重生成、向上懒加载、复制、主题/语言切换、移动端手势）围绕 `chatStore` 与 `authStore`；消息安全渲染依赖 marked + DOMPurify；视觉依赖 CSS 变量主题与粒子背景。

### 7.2 `components/auth/`（1 个）

| 组件 | 职责 | 关键细节 |
|---|---|---|
| `AuthPage.vue` | 登录 / 注册页（props `mode`） | `authStore.login/register/checkUsername`；密码强度评分 `pwScore`（≥8 且 ≥2 类字符）；注册用户名可用性校验；提交后按 `route.query.redirect` 跳转 |

### 7.3 `components/admin/`（11 个）

| 组件 | 职责 | 关键依赖 / 细节 |
|---|---|---|
| `AdminConsole.vue` | 后台布局（侧边导航 + RouterView） | 7 个 tab（含 settings 子路由）；`isActive` 按 `route.path`；`ConfirmModal` 登出确认 |
| `AdminOverview.vue` | 总览仪表盘（6 统计卡 + sparkline + 趋势/小时/模型/年龄/性别/省份图表 + top/最近用户） | ECharts（Bar/Line/Pie）；`fetchOverview`；`onMounted init → resize 监听 → onBeforeUnmount dispose` |
| `AdminUsage.vue` | 用量统计（日趋势 / 模型 / 用户 / 年龄 / 性别） | ECharts + `fetchUsage`；生命周期同 Overview |
| `AdminMap.vue` | 地域分布地图（省级热力 + 市级下钻 + 用户标记点） | ECharts Map/EffectScatter + Geo/VisualMap/Tooltip；`china.json` + `city-coords.json` + `fetchRegionStats` + `provinceHeat`；点击省份 `fetch('https://geo.datav.aliyun.com/areas_v3/bound/${adcode}_full.json')` 下钻；坐标解析多级回退；tooltip 含头像/排名；`watch(currentTheme)` 重建；星空背景用确定性伪随机 |
| `AdminSettings.vue` | 系统设置键值表（key/value/remark/enabled） | `fetchSettings` / `updateSetting`；开关、行内编辑、新增 |
| `AdminSettingsBase.vue` | 基本设置表（样式/逻辑几乎复用 `AdminSettings`） | 同上 |
| `AdminSettingsLayout.vue` | 设置子路由布局（tabs base/search + RouterView） | — |
| `AdminSettingsSearch.vue` | 搜索设置（provider 管理 + searxng 配置 + 高级参数） | provider 拖拽排序；searxng URL/timeout；`testSearxngConnection` 用 `fetch + AbortController` 10s 超时 |
| `AdminSuggestions.vue` | 建议语管理（增删改 + 启用切换） | `createAdminSuggestion/deleteAdminSuggestion/fetchAdminSuggestions/updateAdminSuggestion` + `ConfirmModal` |
| `AdminUsers.vue` | 用户管理表（13 列） | `fetchAdminUsers/resetUserPassword/updateAdminUser`；角色/启用/重置密码/地区/生日/性别编辑；`touchUpdated` 更新 `updated_at/updated_by` |
| `ModelManage.vue` | 模型管理（搜索 + 增改 + 启用 + 删除） | `adminService` + `config/models`（DEFAULT_PROVIDERS）；删除 `ConfirmModal` 确认 |

**admin 模块小结**：`AdminConsole` 布局为入口；图表类（Overview / Usage / Map）统一 ECharts 并规范 `init → resize → dispose` 生命周期；设置类高度复用键值读写；交互类普遍借助 `ConfirmModal` + `useToast`。

### 7.4 `components/common/`（6 个）

| 组件 | 职责 | 接口 / 复用 |
|---|---|---|
| `AppIcon.vue` | Iconify 图标封装 | props `name / size? / glow? / themeFill?` |
| `AppLoading.vue` | SVG 旋转加载器 | props `size? / color? / glow?`（默认 glow） |
| `AppToast.vue` | Toast 容器 | `useToast().toasts` + `AppIcon` |
| `ConfirmModal.vue` | 通用确认弹窗（Teleport） | props `visible/title/message/confirmText?/cancelText?/danger?/confirming?`；emits `update:visible / confirm / cancel`；Esc 取消 / Enter 确认 / Tab 焦点循环；被 chat/admin 广泛复用 |
| `DatePicker.vue` | 日期选择浮层（Teleport） | props `modelValue?/placeholder?/min?/max?/disabled?`；emits `update:modelValue`；中/英星期、`reposition`；被 UserProfileDialog / AdminUsers 复用 |
| `RegionSelect.vue` | 省/市/区三级联动 | props `modelValue: RegionValue / disabled? / vertical?`；emits `update:modelValue`；导出 `RegionValue { province, city, district }`；依赖 `region-data.json`；被 UserProfileDialog / AdminUsers 复用 |

**common 模块小结**：无业务耦合的基础组件，依赖仅工具 / composable；以 `v-model` + Teleport + 键盘事件实现标准化交互，被 chat 与 admin 模块广泛复用（尤其 ConfirmModal / DatePicker / RegionSelect / AppIcon / AppLoading / AppToast）。

---

## 8. 关键业务流程

### 8.1 登录流程
1. 用户访问 `/` → 守卫 `auth.init()`：无 token → 无 user → 重定向 `/login`。
2. `AuthPage` 提交 → `authStore.login(username, password)` → `authService.login` 拿 `{token, user}` → `setToken` + `fetchMe`。
3. 守卫放行 → 进入 `/chat`（或 `route.query.redirect`）。

### 8.2 发送消息（流式）
1. `ChatWindow.handleSendMessage` → `InputBox` emit `send` → `chatStore.sendMessageStream`。
2. 乐观插入 user + 空 assistant 消息（联网搜索态）。
3. `buildHistory`（≤20 条）→ `apiService.chatStream`（`POST /api/chat`，SSE）。
4. 每收到分片：`onChunk` 累加内容 → 首包关闭搜索态 → `MessageItem` 实时渲染 + 流式光标。
5. `[DONE]` → `finally` 解除 loading + `refreshSessions()`；异常写入错误文本；用户可 `abortCurrentRequest` 取消。

### 8.3 管理后台地图热力
1. `AdminConsole` → `/admin/map` → `AdminMap` 挂载。
2. `loadStats()` → `adminService.fetchRegionStats(period)` → `RegionStatsData`（provinces / regions）。
3. `computeProvinceHeat` 算每省 0~100 热度 → `visualMap` 配色 → 省级热力图。
4. 点击省份 → 拉 datav 市级 GeoJSON 下钻；`effectScatter` 标 top 用户（同坐标 9 宫格偏移防重叠）；`watch(currentTheme)` 重建实例以随主题变色。

---

## 9. 前端调用的后端接口契约汇总

> 以下路径均由前端 `services/` 调用，经 `/api` 代理到 `localhost:8000`。带 `*` 表示需 Bearer Token。

**鉴权 / 用户（`authService`，`/api`）**
- `POST /api/auth/login` → `{ token, user }`
- `POST /api/auth/register` → `{ token, user }`
- `GET  /api/auth/check-username?username=`（公开）→ `{ available }`
- `GET  /api/auth/me` * → `AuthUser`
- `PATCH /api/auth/me` * → `AuthUser`（更新资料）

**聊天 / 会话（`apiService`，`/api`）**
- `POST /api/chat`（SSE）* → 流式分片（OpenAI 兼容）
- `GET  /api/models`（公开）→ `ModelInfo[]`
- `GET  /api/suggestions`（公开）→ `ChatSuggestion[]`
- `GET    /api/sessions` * → `ChatSession[]`
- `POST   /api/sessions` * → `ChatSession[]`
- `PATCH  /api/sessions/:id` * → `ChatSession[]`
- `DELETE /api/sessions/:id` * → `ChatSession[]`
- `DELETE /api/sessions/:id/messages` * → `ChatSession[]`
- `GET  /api/sessions/:id/messages?before_id=` * → `{ messages, has_more }`
- `DELETE /api/sessions/:id/messages/:mid` * → `ChatSession[]`

**后台（`adminService`，`/api/admin`，全部需管理员 Token）**
- `GET    /admin/stats` → `AdminStats`
- `GET    /admin/overview` → `AdminOverview`
- `GET/POST   /admin/models`；`PUT/DELETE /admin/models/:id` → `AdminModel[]`
- `GET    /admin/users`；`PATCH /admin/users/:id`；`POST /admin/users/:id/reset-password`
- `GET    /admin/usage` → `AdminUsage`
- `GET    /admin/region-stats?period=` → `RegionStatsData`
- `GET/PUT    /admin/settings`；`PATCH /admin/settings/:key`
- `GET/POST   /admin/suggestions`；`PUT/DELETE /admin/suggestions/:id`

---

## 10. 设计亮点与可改进点

**亮点**
- 分层清晰：视图 / 状态 / 服务 / 类型解耦，服务层统一 Token 与 401 处理，可维护性好。
- 流式体验扎实：三层超时 + AbortController 取消 + 乐观更新 + 搜索态过渡，健壮性高。
- 主题 / 国际化 / 设备适配全面，且均通过 CSS 变量与 store 联动，视觉与逻辑一致。
- 管理后台产品化：概览 / 用量 / 地图热力 / 用户 / 模型 / 建议 / 设置一应俱全，图表生命周期规范。
- 安全细节：Markdown 走 DOMPurify 防 XSS；复制操作有 Clipboard API + execCommand 兜底；坐标解析多级回退。

**可改进点（供参考）**
- `apiService` 同时承担会话 CRUD 与聊天流，体量偏大，可按领域拆 `sessionService` / `chatService`。
- `AdminSettings.vue` 与 `AdminSettingsBase.vue` 逻辑近乎重复，可抽公共组件或复用同一实现。
- 坐标解析依赖外网 `geo.datav.aliyun.com`，离线 / 弱网环境下钻会失败（已有失败提示，但可考虑内置市级 GeoJSON 缓存）。
- `apiService` 的错误文案部分硬编码中文、部分走 i18n，建议统一走 `i18n.global.t`。
- 类型 `ApiConfig`（provider/baseUrl/key…）在前端声明但未使用（密钥在后端），可移除或标注为后端契约，避免误导。
- 组件内联 `fetch`（如 `AdminSettingsSearch.testSearxngConnection`）绕过了统一 service，建议收敛到 `adminService`。

---

> 本文档基于源码静态分析生成，覆盖 `src/` 全部模块到组件级粒度；运行时行为（如后端实际返回结构）以第 9 节契约为准，联调时以后端为准。
