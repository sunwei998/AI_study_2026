# 2026-08-27 工作日志

## AppInput 聚焦异常修复（bug）
- 现象：数字输入框聚焦时出现「双层边框 + 发光环」的怪异效果。
- 根因：`src/styles/global.css` 里的全局 `input:focus` 规则带 `box-shadow: 0 0 0 3px var(--color-glow), 0 0 18px ...`，特异性高于组件内 `.app-input__inner`（未写 box-shadow），聚焦时穿透到内部 `<input>` 上。
- 修复：`AppInput.vue` 的 `.app-input__inner` 补 `box-shadow:none; padding:0; border-radius:0`，并新增 `.app-input__inner:focus` 覆盖全局 focus 样式。
- 顺带移除聚焦时 step 内部边框变主色的两段样式（`.is-focused .app-input__step` / `.step-btn:first-child`）。

## 用户管理表格用 AppTable 替换（试验）
- 需求：用封装好的通用表格 `src/components/common/AppTable.vue` 替代 `AdminUsers.vue` 原生 `<table>`，支持表头筛选「用户名」「角色」。
- 改动 `AdminUsers.vue`：
  - 用 `<AppTable>` 替换原生表格，`size="small"`、`loading-type="skeleton"`、`row-key="id"`。
  - 新增 `columns` computed（`TableColumn[]`）：用户名列 `filterable + filterType:'input'` + `filterMethod`（username 模糊匹配）；角色列 `filterType:'select'` + `filters: [admin,user]`（文案用 `console.roleAdmin/roleUser`）。
  - 自定义渲染用具名作用域插槽：`#column-username`（self-tag）、`#column-role`（role-badge）、`#column-is_active`（toggle）、`#column-actions`（fixed:'right' 操作按钮）。
  - 纯文本列用 `formatter`；数字/时间/地区列加 `className`（cell-num/cell-time/cell-region），样式用 `:deep()` 作用到 AppTable 内部 td。
  - 布局：`.users-table-wrap` 改为 flex 容器，`:deep(.app-table-wrapper/.app-table-scroll)` 设 `flex:1; min-height:0` 以撑满并内部滚动。
- 移除 `TableLoading` import（改用 AppTable 骨架屏）。

## 修复既有类型错误（让 build 通过）
- `AppTable.vue` 排序逻辑 `col.sorter!(a,b)` 报 TS2349（sorter 类型 `boolean | fn`）：改为先取 `const sorter = col?.sorter` 再 `typeof === 'function'` 收窄。
- `Pagination.vue` `@update:model-value="changePageSize"` 类型不匹配：改为 `(v) => changePageSize(Number(v))`。
- 结果：`vue-tsc --build` 通过，`vite build` 成功。

## 复用知识
- 全局 `input/textarea:focus` 样式会穿透到封装组件内部 `<input>`（特异性 `input:focus` 0,1,1 > `.class` 0,1,0），封装输入类组件务必显式写 `.xxx:focus { box-shadow:none }` 覆盖。
- AppTable 用法要点：列筛选 `filterable + filterType(input/select/checkbox/radio) + filters/filterMethod`；自定义单元格用 `#column-{key}` 作用域插槽 `{row,index,value,column}`；固定列 `fixed:'right'`；外层撑满需 `:deep` 给 wrapper/scroll 设 flex。

## 用户列表改为服务端筛选（用户名 + 性别）
- 需求：筛选字段由「用户名+角色」改为「用户名 + 性别」，性别用 AppCheckbox（多选）、用户名用 AppInput（模糊），且**后端过滤**。
- 后端 `ai-chat-server/ai_chat_server/admin.py` `list_users`：新增 `username`（LIKE 模糊）和 `gender`（逗号分隔多选，拼 `IN (?)`）两个 Query 参数；`where` 由 clauses 列表拼 ` AND `，`search` 保留兼容。
- 前端 `adminService.ts`：`PaginationParams` 加 `username?: string`、`genders?: string[]`；`fetchAdminUsers` 拼 `username`、`gender=a,b`。
- 前端 `AdminUsers.vue`：
  - gender 列 `filterable + filterType:'checkbox'` + `filters: [male/female/other]`（文案 `auth.genderMale/Female/Other`）；role 列去掉 filterable。
  - 新增 `usernameFilter`/`genderFilter` 状态，`load()` 透传；`onFilterChange(filters: Record<string, any[]>)` 从 `filters.username[0]`、`filters.gender[]` 取值，重置 page=1（已是 1 则手动 load 避免双重）。
  - AppTable 加 `@filter-change="onFilterChange"`。
- 服务端筛选模式：AppTable 内部仍会前端过滤 data，但 data 已是后端过滤结果，幂等无害。
- 验证：前端 vue-tsc + vite build 通过；后端 py_compile 通过。

## 修复表头筛选图标不显示 + 补回角色筛选
- 现象：用户列表只显示 1 个筛选图标（性别），用户名和角色都没有。
- 根因：`AppTable.vue` 表头筛选按钮条件写成 `v-if="col.filterable && col.filters?.length"`，`filterType:'input'`（用户名）没有 `filters` 数组 → 按钮被隐藏。改为 `v-if="col.filterable"` 即可让 input 类型也显示漏斗按钮。
- 补回角色筛选：role 列加回 `filterable + filterType:'select'` + `filters:[admin,user]`（文案 `console.roleAdmin/roleUser`）。
- 后端 `list_users` 追加 `role` 参数（逗号分隔多选，`u.role IN (?)`）；前端 service `PaginationParams` 加 `roles?: string[]`、`fetchAdminUsers` 拼 `role=a,b`；AdminUsers 加 `roleFilter` 状态并在 `onFilterChange` 里取值透传。
- 最终筛选字段：用户名(input)、角色(select)、性别(checkbox)，全部服务端过滤。

## 修复 AppTable 筛选面板封装缺陷（多选 + 全部 + 样式）
- 问题：① 角色 select 太窄（AppSelect 是 inline-block 无 width:100%，在 200px 面板里只占 80px）；② select 下拉面板被 `.app-table__filter-list` 的 `overflow-y:auto` 裁剪遮挡；③ 要求 checkbox 多选且默认带「全部」选项。
- `AppTable.vue` 筛选面板改造：
  - checkbox 分支改为**纵向原生 checkbox 列表**（复用 `.app-table__filter-item`），顶部加「全部」项（`:checked="selected.length===0"`，点击 `onAllFilterChange` 清空 selected）；不再用横向的 `AppCheckboxGroup`。
  - select 分支改用独立容器 `.app-table__filter-select`（无 overflow），`:deep(.app-select){display:block;width:100%}` 铺满；移除 AppSelect 不支持的 `size`/`clearable` prop。
  - 删除 unused 的 `checkboxOptions`、`onCheckboxGroupChange`、`AppCheckboxGroup`/`CheckboxOption` import。
  - 新增 `.app-table__filter-item--all` 样式（primary 色 + 下边框分隔）。
- 角色列 `filterType` 由 select 改为 checkbox（多选）。
- 「全部」交互：selected 为空时勾选；勾选具体项自动脱离；点「全部」清空筛选。

## 修复 AppTable 筛选面板"再次点击图标无法收起"的 bug
- 现象：打开某列表头筛选面板后，再次点击同一图标，面板不收起（保持打开）。
- 根因：事件阶段冲突。图标 `@click.stop="toggleFilter"` 只拦截**冒泡**阶段；而 `onFilterDocClick` 用 `document.addEventListener('click', ..., true)`（capture=true）注册，在**捕获**阶段早于图标 `@click` 触发。二次点击同一图标时：捕获阶段 `onFilterDocClick` 先把 `visible` 置 false 并移除监听 → 随后 `toggleFilter` 见 `visible` 为 false 落入「打开」分支，把面板重新打开。
- 修复：在 `onFilterDocClick` 开头加 `if ((target as Element)?.closest?.('.app-table__filter-wrap')) return`，让点击筛选图标本身时交给 `toggleFilter` 处理开关。`toggleFilter` 内 `if (visible && sameCol) closeFilterPanel()` 逻辑已正确，无需改。
- 验证：vue-tsc + vite build 通过。

## AppTable 表头筛选支持 radio（复用 AppRadio.vue）+ 应用到启用列
- 需求：表头筛选新增 radio 类型，且复用在 `src/components/common/AppRadio.vue`（主题色科技风），应用到用户管理「启用列」。
- `AppTable.vue` 改造：
  - 原 radio 分支用 `AppRadioGroup`（其内联渲染且样式依赖 AppRadio.vue 的 scoped 类、实际未正确套用主题），改为直接渲染 `AppRadio`：顶部一个 value='' 的「全部」项（`app-table__filter-item--all`，modelValue 用 `selected.length===0?'':null` 表示未筛选），下方按 `col.filters` 渲染各 `AppRadio`；同一 `name=app-table-filter-{colKey}` 保证原生单选互斥。
  - 删除 `AppRadioGroup`/`RadioOption` import、`radioOptions` computed、`onRadioGroupChange`，新增 `radioGroupName` computed 与 `onRadioSelect(value)`（value 为空→selected=[]，否则 [value]）。
  - 样式：`.app-table__filter-list :deep(.app-radio){display:flex;width:100%}` 让 radio 纵向铺满；`.app-table__filter-item--all :deep(.app-radio__label){color:var(--color-primary)}` 让「全部」文字与主色一致。
- `AdminUsers.vue`：`is_active` 列加 `filterable + filterType:'radio' + filters:[{启用:true},{禁用:false}]`（文案 `console.enabled/disabled`）；新增 `isActiveFilter` 状态，`load()` 透传 `isActive`（null=不过滤），`onFilterChange` 里从 `filters.is_active[0]` 取布尔值。
- 后端 `admin.py` `list_users` 新增 `is_active` 参数：`u.is_active = ?`（字符串 true/false 转 1/0，适配 SQLite）。前端 `adminService.ts` `PaginationParams` 加 `isActive?`，`fetchAdminUsers` 拼 `is_active=true|false`。
- 验证：vue-tsc + vite build + 后端 py_compile 全通过。
- 注：后端改 `list_users` 需重启 FastAPI 才生效（若未开 --reload）。
- **BUG 修复（is_active 筛选空数据）**：根因不是前后端传参，而是 `AppTable.vue` 的 `processedData`（内部前端过滤）用 `values.includes(getCellValue(row,col))` **严格相等**比较；后端 SQLite 返回 `is_active` 是数字 1/0，筛选值是布尔 true/false，`[true].includes(1)`=false → 全部行被前端过滤掉 → 「启用/禁用都空，全部有数据」。修复：`AdminUsers.vue` is_active 列加 `filterMethod: (v,row) => Boolean(row.is_active) === Boolean(v)` 宽松比较。教训：AppTable 是「data+前端过滤」通用组件，即使服务端筛选，父组件拿回的数据仍会被内部 processedData 再过滤一遍；布尔/数字类型列必须配 filterMethod。
- **AppTable 排序能力升级**：排序图标从标题内拆成独立按钮 `.app-table__sort-btn`（22x22、hover/active 主色，与筛选按钮风格统一），位置固定「标题 → 排序 → 筛选 → 拖拽」；删掉 `.app-table__filter-wrap{margin-left:auto}`，`onFilterDocClick` 的 closest 选择器同步改为 `.app-table__filter-btn`。新增 props：`customSort`（布尔，服务端排序模式：processedData 跳过内部排序）、`sortMethod?(key, order)`（点击排序时的回调，供外部处理后端请求）；`toggleSort` 末尾调用 `props.sortMethod?.(key, order)`。前端排序沿用 `sortable/sorter` + processedData；后端排序用 `customSort` + `sortMethod`。
- **后端 list_users 排序**：新增 `sort`（字段白名单 username/role/is_active/gender/birthday/created_at/updated_at/last_seen_at/logins/total_tokens）与 `order`（asc/desc，默认 desc），非法 sort 回退 `u.created_at DESC` 防注入。前端 adminService `PaginationParams` 加 `sort/order`。AdminUsers 演示：logins 列 `sortable:true`，AppTable 配 `custom-sort :sort-method="onServerSort"`，onServerSort 存 sortFilter 并重置页码 load。
- 验证：vue-tsc + vite build + py_compile 通过；curl 实测 logins desc=[144,9,2,1,1]、asc 正确、非法字段回退默认排序。
- **表头分割线 + 列宽**：表头列间分割线改为 `::after` 伪元素（`top:50% + translateY(-50%)`、高度 50%、1px、`--color-border` 45% 透明），**中间一段垂直居中**，不再顶天立地；`:last-child::after` 隐藏；`fixed-left` 列的 `::after` 阴影特异性更高自然覆盖分割线。用户管理列宽调整：role/gender/is_active 90→110、logins 70→110、total_tokens 90→110、updated_by 120→130；随后继续加宽：username 190、role/gender/is_active/logins/total_tokens 130、birthday 150、region 160、时间列 200、updated_by 150、actions 140。再一轮（除 role/region/actions 外）：username 220、birthday/updated_by 170、gender/is_active/logins/total_tokens 150、时间列 220。再再一轮：birthday 200、三个时间列 250。之后：birthday + 三个时间列（last_seen_at/created_at/updated_at）全部加 `sortable:true`（走既有 customSort+sortMethod 后端排序，后端白名单本就支持），实测 created_at asc/desc、birthday、last_seen_at 排序正确。注意：后端登录接口返回字段是 `token`（不是 access_token）。
- **角色体系扩展（5 角色）**：super_admin/system_admin/model_admin/user/subscriber。后端 auth.py 加角色常量 ADMIN_ROLES + `require_admin`（放宽为 3 管理角色）+ `require_super_admin`（仅 super_admin）/`require_model_admin`（super+model）/`require_settings_admin`（super+system）；admin.py 端点级叠加：users 写操作→super、models 写→model_admin、settings 写→settings_admin，GET 全部保持 require_admin（只读对所有管理角色放行）；schemas UserUpdate.role 白名单 5 值；chat.py `_check_model_access`（user 角色禁付费模型 403）；main.py lifespan 幂等迁移 `admin→super_admin`；seed 角色改 super_admin。实测：system_admin 读 users 200/写 403、model_admin 写 models 放行/写 settings 403、user 付费模型 403、subscriber 放行 200。
- 前端：types/admin.ts UserRole 5 值；新建 `src/utils/roles.ts`（ADMIN_ROLES/ALL_ROLES/ROLE_LABEL_KEYS/isManagerRole/isSuperAdminRole）；authStore `isAdmin→isManager` + `isSuperAdmin`；router meta `requiresAdmin→requiresManager`；UserMenu/ChatWindow 管理入口改 isManager；AdminUsers：5 角色徽章（role-super_admin 金/system_admin 青/model_admin 紫/subscriber 绿/user 中性）、筛选 5 选项、角色切换改弹窗 AppSelect（仅 super_admin 显示操作列，toggle 对非 super_admin 禁用）；ModelManage 非 model_admin 隐藏操作；AdminSettingsBase/Search 非 settings_admin 只读（输入 disabled、保存隐藏、拖拽拦截）；chatStore `allModels` + `availableModels` computed（user 角色过滤 free）；新建 `src/services/forbidden.ts` + main.ts setForbiddenHandler → 403 全局 toast `common.noPermission`（apiService/adminService 403 触发）。
- **BUG 修复：AppSelect 下拉被遮挡**：`.as-panel` 原为组件内 absolute 定位，父容器 `overflow:hidden`（Pagination `.pagination`）或滚动容器（弹窗 form-modal）会裁剪它。重构：下拉面板 **Teleport 到 body + fixed 定位**（JS 用 trigger getBoundingClientRect 计算 top/left/minWidth，视口底部放不下时向上翻，window scroll(capture)/resize 时跟随更新，z-index 3000，点击面板内不关闭）。一次修复分页每页条数下拉/角色弹窗等所有场景。
- **主题色块修复（已全部回退）**：用户最终要求回退今天所有主题色块更改——已删除 `src/utils/themeSwatches.ts`，UserMenu.vue / ThemeSwitcher.vue 恢复硬编码色块 CSS（theme-dot / theme-preview + 8 主题），themes.ts mint 恢复 `#00e676`。教训：用户说的「色块」仅指主题切换面板里的展示色块，不要动主题本身；双份硬编码色块颜色（UserMenu 与 ThemeSwitcher 各一份）是历史遗留，后续若要统一需用户明确同意。**随后按用户要求只改 mint 色块**为「发亮的渐变墨绿」：`.theme-dot.mint` / `.theme-preview.mint` → `linear-gradient(135deg, #00352b 0%, #00e676 70%, #b7ffde 100%)`（墨绿→主题亮绿→高光），桌面端与移动端同步，主题本身未动；后按用户要求再加深加黑为 `#001a12 → #00b359 → #9dffd4`。
- **AppTable 表头液态玻璃风**：对齐数据字典——`--font-mono` 11px、0.08em 字距、`var(--color-glass)` 背景、blur(8px)；随后更激进：加 `glass-sheen` 渐变 + blur(12px) saturate + 默认表头 padding 12px→14px（small/large 变体不变）。
- **模型管理/数据字典切 AppTable**：ModelManage 重写——去顶部文本框搜索，改表头筛选（enabled/free radio + provider checkbox）+ 排序（model_key/name/provider/sort_order），custom-sort+sort-method+filter-change 服务端模式，操作列/开关/设默认/表单保留；后端 `list_models` 加 `enabled/free/provider(IN)/sort/order`（白名单，非法回退 sort_order,id）。AdminSettingsBase 重写——key 排序 + enabled radio 筛选，value/remark 用插槽 AppInput 编辑、enabled switch、保存按钮，后端 `list_settings` 加 `enabled/sort/order`。adminService `PaginationParams` 加 `enabled/free/providers`。i18n 加 console.paid。
- **热词页**：AdminHotWords 列表（排名/词/次数）换 AppTable 纯展示（无排序筛选）；菜单文案 `console.hotWords` 由「高频词分析」改「热词分析」。
- **用量页码表**：去掉性别饼图，新建 `src/components/common/AppGauge.vue`（SVG 270° 码表：渐变发光进度弧 + 刻度 + 指针 + 渐变大数字，CSS 变量跟随主题，stroke-dashoffset/transform 缓动动画）；AdminUsage 用其显示「今日 Token 消耗」（daily 末天 total，量程=max*1.15），caption 显示速率 `console.tokenRate`。i18n 加 todayTokens/tokenRate。
- 验证：vue-tsc + vite build + py_compile 通过；实测 models sort_order/model_key 排序、free/enabled/provider 过滤、settings key 排序、非法 sort 回退均正确。注意 models 表 provider 实际值仅 openai/tencent（与 model_key 前缀无关）。
- **AppTable 页面滚动修复**：模型管理/数据字典（凡 AppTable + Pagination 的页面）需三层 flex 约束让表格内部滚动、分页器固定底部：`.wrap{flex:1;min-height:0;display:flex;flex-direction:column}` + `:deep(.app-table-wrapper)` 同 + `:deep(.app-table-scroll){flex:1;min-height:0}`（参照 AdminUsers）。**Pagination 间距**：去掉 `.pagination` 的 `margin-top:12px`（原与父容器 gap:14px 叠加成 26px），统一由各页面父容器 gap 控制（≈14px）。
- **AppButton 组件**：新建 `src/components/common/AppButton.vue`——size mini(26)/small(32)/middle(38)/large(44)、type primary(主题渐变+霓虹发光)/default(液态玻璃 blur+saturate)、loading(继承 currentColor spinner)、disabled/block；圆角 var(--radius-md)。应用到：模型管理「新增」、数据字典「新增」、搜索引擎设置 3 个保存按钮。
- **文案/图标**：`console.overview` 概览→数据概览；`settingsSearch` 搜索引擎设置→搜索引擎；`addModel` 新增模型→新增；`addSetting` 新增设置→新增（中英同步）；AdminSettingsLayout 数据字典 tab 图标 sliders-horizontal→book-open；数据字典 toolbar 加 margin-top:4px（离 tab 稍远、离表格同模型管理）。
- **age_dist 改 token**：后端 `_aggregate_demographics` 年龄分布由人数改为按年龄分组 token_usage 总量（LEFT JOIN + SUM），性别分布仍按人数；前端 age 饼图 label/tooltip 用 formatTokens。实测 25-34 组 1320955 tokens、gender 仍 male4/female2/unknown9。
- **数据修正**：8 个 user 的 updated_by 由各自用户名改为 'system'。
- **头像裁剪重构（AvatarCropDialog.vue）**：根因是 baseScale 用 `Math.max`（cover），大图初始即放大到最小边填满圆框导致「只能选中间一小块」。重构为主流交互：baseScale 改 `Math.min`（contain，初始完整显示整张图）、minZoom=1/maxZoom=4、拖拽 + 双指缩放（Pointer Events 多指跟踪）、缩放后保持中心并 clamp 边界、实时 1:1 预览圆（背景映射）、九宫格辅助线、主题色发光圆环、科技风滑块 + 百分比提示、stage 尺寸响应式、移动端隐藏预览。i18n 加 `profile.cropTip`。
- **搜索页保存按钮**：AppButton size middle→small（3 处）。
- **用量页 age 饼图 → 调用次数码表**：去掉 ageRef 渲染/ageBuckets/ageLabel/pieColors/PieChart/LegendScrollComponent，新增 reqGauge（daily 末天 requests，caption 近30天总调用次数），与 Token 码表并排。i18n 加 todayRequests/requestRate。
- **按钮组间距**：模型管理/数据字典 toolbar `margin-bottom:-8px`（父 gap14 − 8 = 6px 到表格）；数据字典 toolbar `margin-top:10px`（离顶部 tab 更远）。
- **头像裁剪移动端 bug 修复**：移动端双指缩放用 pointer 事件会被浏览器手势接管（pointercancel）打断导致 zoom 未更新、保存成「全图」。重构：改用 **touch 事件（@touchstart/touchmove.prevent 阻止默认手势）+ mouse 事件**替代 pointer；confirm 改为 **DOM 实测**（imgRef.offsetLeft/offsetWidth + stage.clientWidth 映射源图坐标），不依赖 computed 状态，PC/移动端一致。
- **导入/导出组件**：新建 `src/components/common/AppImport.vue`（拖拽到框内导入：dragover/drop + 点击选择、accept/multiple/maxSize 校验、文件列表、液态玻璃发光拖拽框）+ `AppExport.vue`（导出按钮，mini/small/middle/large + loading）+ `src/utils/download.ts`（downloadTextFile/Json/Csv/Blob + formatFileSize）。先封装未接入页面。
- **导入/导出二级菜单**：系统设置新增「导入管理」「导出管理」两个二级菜单（AdminConsole settings 分组 children + AdminSettingsLayout tabs + router 子路由），页面为 AdminSettingsImport/Export 空占位；i18n 加 importManage/exportManage/comingSoon。
- **热词分析 3D 化**：安装 echarts-gl，AdminHotWords 横向条形图改为 **bar3D 三维柱状图**（主题色→强调色按高度渐变、光影/阴影、可旋转视图、emphasis 高亮 + label），图表固定高度 420px；表格保留 AppTable。注意 AdminHotWords chunk 因 echarts-gl 增至 628KB。

## 热词图表重设计 + 数据管理菜单 + 导入导出记录（第 N 轮）
- **热词图表回退 3D**：用户不满 bar3D（可拖拽缩放、太原生、丑）。改为**纯 CSS 发光排行榜**（移除 echarts/echarts-gl 依赖）：前三名金银铜徽章 + 主题色渐变发光条形（宽度按 maxCount 比例）+ 等宽字体计数，无拖拽缩放，有限空间清晰展示；`npm uninstall echarts-gl` 已卸载。
- **新一级菜单「数据管理」（四字）**：导入管理/导出管理从「系统设置」移出，改挂到新一级菜单 data（icon lucide:database）下；新增 `AdminDataLayout.vue`（与 AdminSettingsLayout 同款 tab 布局）+ `AdminImportRecords.vue`/`AdminExportRecords.vue`（薄包装）+ 共用 `AdminTransferRecords.vue`（type prop 区分）；路由 `/admin/data/import|export`；删除旧 AdminSettingsImport/Export 占位页。
- **概览改名**：overview 文案 `数据概览`→`概览`（中英）。
- **导入/导出记录（后端）**：db.py 新增 `transfer_records` 表（type=import/export、user_id、username、filename、file_size、mime_type、file_path、remark、created_at）+ idx；admin.py 新增 `GET /api/admin/transfers`（type 过滤 + 分页 + sort 白名单）与 `GET /transfers/{id}/download`（FileResponse，防路径穿越 + has_file 标记）；所有管理员可见（router 级 require_admin）。源文件存 `data/transfers/`。
- 前端 adminService 新增 `fetchTransfers` / `downloadTransfer`（fetch+blob 触发下载）；types 加 TransferRecord/TransferType；记录页 AppTable（文件名/操作人/大小/备注/时间 + 固定下载列，has_file 决定禁用）+ 三层 flex + Pagination。
- i18n 新增 dataManage/transferFile/transferOperator/transferSize/transferRemark/transferTime/downloadFile/noSourceFile/noImportRecords/noExportRecords。
