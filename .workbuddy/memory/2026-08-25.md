# 2026-08-25

- 对 vue_vite_demo（Vue3+TS+Vite AI 聊天 SPA）做了逐文件深度研究，产出模块级技术文档 `技术文档.md`（覆盖入口/路由/状态/服务/类型/配置/i18n/样式/composables/utils/30 个组件 + 后端接口契约汇总 + 设计亮点与改进点）。
- 关键架构事实：前端只做 UI/状态/流式渲染/鉴权，业务与模型调用委托给 `localhost:8000` 后端；`/api` 由 Vite 代理；401 统一处理重置登录态；SSE 流式三层超时（首包30s/空闲60s/总5min）；8 套 CSS 变量主题；管理后台含中国地图省级热力+市级下钻（datav GeoJSON）。
- 后台侧边栏迭代（AdminConsole.vue）：「基础设置」改名「数据字典」（zh-CN 文案 + en-US 补 settingsBase/settingsSearch 键）；一级菜单左对齐（分组头 space-between→flex-start + 箭头 margin-left:auto）；二级菜单先去掉冒前（margin-left 39px 落 63px），后按用户要求与一级文字完全对齐（margin-left 27px 落 51px），并移除二级左侧竖线（border-left）。
- ECharts 饼图 legend 翻页：AdminUsage.vue / AdminOverview.vue 的年龄饼图 legend 改为 `type:'scroll'` 横向翻页（需额外 import+注册 LegendScrollComponent；Overview 的 renderDonut 加 scrollLegend 参数只对 age 生效），性别饼图保持 plain。vue-tsc 通过。
- 管理控制台侧边栏折叠优化（AdminConsole.vue）：用户嫌底部大按钮（.console-collapse-btn）笨重且动画卡顿。改为极简「图钉」式切换：移除底部整块 .console-nav-footer/.console-collapse-btn，新增 26×26 圆角小圆钮 .console-collapse-pin（position:absolute; top:18px; right:-13px，常驻锚定在侧栏右边缘、跨分界线上，含 chevron 图标，hover 高亮；title/aria-label 复用 console.collapse/expand 文案）。流畅性关键修复：删掉 .console-nav 上的 backdrop-filter 模糊（侧栏背后仅页面背景、模糊无可见效果却导致 width 动画每帧重绘卡顿），加 will-change:width、过渡仅作用于 width 并用 cubic-bezier(.4,0,.2,1) 0.24s；图钉自身 transform 合成。保留 collapsed+localStorage 持久化与收起态点击分组自动展开；移动端 @media(max-width:768px){.console-collapse-pin{display:none}}。vue-tsc 通过、Vite 200。
- AdminUsage.vue 用量页：新增 `formatTokens` 把 token 大数紧凑格式化（≥1e8→X亿/≥1e4→X万/≥1e3→Xk）；按模型、按用户两个横向柱状图的 xAxis 轴标签与柱顶 label 均套用该 formatter，解决横轴长数字拥挤。vue-tsc 通过。
- 单位体系 K/W/M 全站统一：AdminUsage `formatTokens`（≥1e8→X亿/≥1e6→XM/≥1e4→XW/≥1e3→XK）已用于按模型/按用户横轴+柱label+近30天趋势双y轴+tooltip；AdminOverview `fmtTokens` 升级成同规则并用于按模型横轴+近30天趋势双y轴+tooltip+数字卡；AdminMap `fmtTokens` 也升级为 K/W/M 并用于 tooltip 与 visualMap 端点；本轮补齐 AdminOverview 遗漏的 hourly y 轴、省份 x 轴（均加 formatter + tooltip valueFormatter）。全 admin value 数据轴现已统一 K/W/M（仅 sparkline 隐藏轴跳过）。vue-tsc 通过。
- 年龄/性别饼图国际化排查：后端 `_aggregate_demographics` 返回中性 key（'0-17'/'male'…），前端 `ageLabel`/`genderLabel` 已走 `console.age*`/`auth.gender*` 翻译且 zh/en 键齐全——静态渲染本就国际化。真正问题是图表只在 onMounted 画一次、ECharts 画布不响应 t() 变化，切语言后饼图图例/label 不重绘。修复：AdminUsage.vue 与 AdminOverview.vue 均引入 `watch` 监听 `locale.value` 变化后重绘（Usage→render()，Overview→renderAll()）。vue-tsc 通过。
- 管理控制台收起态交互改为主流方案（AdminConsole.vue，参考 VSCode/Ant Design Pro/Vben Admin）：
  - 分组图标收起态**点击无任何反应**（toggleGroup 开头 `if (collapsed.value) return`，去掉原先"自动展开侧栏"逻辑）；改为**悬停弹出气泡**：`.console-group-flyout`（绝对定位 left:calc(100%+10px)、z-index:50、玻璃卡片，含分组名 header + 二级 RouterLink，`::before` 透明连接带 10px 防悬停中断；max-height:60vh 防溢出），在 `@media(min-width:769px)` 内 `.is-collapsed .console-menu-group:hover` 时显示。
  - 普通项（无子级）包一层 `.console-menu-item-wrap`（position:relative），收起态悬停显示标签 tooltip `.console-item-tip`。
  - 模板结构：普通项改为 wrap 包 RouterLink + tip；分组新增 flyout 节点（class 用 `console-flyout-item`，避免被 `.is-collapsed .console-menu-item span{display:none}` 误隐藏）。
  - 卡顿再优化：移除 `.console-nav` 的 `will-change:width`（width 动画无法合成、反而多一层）与图钉的 `will-change:transform`，过渡缩短为 `width 0.18s ease-out`。
  - 图钉位置回顾：right:-13px（跨线）→ -28px（完全在分割线外侧）→ 因被页头(z-index:30)盖住不可见，改 z-index:40 浮于其上。vue-tsc 通过、Vite 200。
- 设备类型区分重构（AdminConsole.vue + index.html）：用户要求移动端/PC 端不要用 CSS 媒体查询，改走「html 独有的参数」→ 在 index.html `<head>` 加内联脚本，按 UA（/Mobi|Android|iPhone|iPad|iPod|IEMobile|Opera Mini|BlackBerry/）或（maxTouchPoints>0 && ontouchstart）判定设备，给 `<html>` 打 `data-device="mobile|desktop"`。AdminConsole.vue 里全部 `@media (max-width:768px)` / `(min-width:769px)` 改为 `[data-device="mobile"]` / `[data-device="desktop"]` 选择器（移动端 60px 图标栏+隐藏图钉；desktop 收起态+悬停气泡）。注意这是设备级区分，桌面窄窗口仍按 desktop 处理（220px 侧栏）。图钉位置改为底部：`bottom:20px; right:-26px`（左缘恰贴分割线、无缝相接，GitHub 式底部收缩钮）。vue-tsc 通过、Vite 200。
- AdminConsole 图钉再调：用户要求把按钮放进面板内部、右缘贴分割线（right:0）、连接处无圆角（border-radius:8px 0 0 8px，右侧直角）。移动端二级菜单兼容：新增 `flyoutOpen` ref + `isMobileDevice()`（读 html data-device）；`toggleGroup` 逻辑改为「PC收起态点击无动作 / 移动端点图标弹二级菜单气泡（flyout-open class 控制显隐）/ 桌面展开态内联展开」；document click 监听点击气泡外部关闭 + watch(route.path) 跳转后自动关闭；移动端隐藏内联二级菜单（`[data-device="mobile"] .console-menu-group-children{display:none}`）并新增 `[data-device="mobile"] .console-menu-group.flyout-open .console-group-flyout` 显隐规则。vue-tsc 通过、Vite 200。
- 移动端样式错乱修复（AdminConsole.vue + index.html）：用户反馈移动端仍显示一级菜单名（「设置」分组头）且错乱——根因是 `[data-device="mobile"]` 块漏掉分组头 `.console-menu-group-header`（旧媒体查询同样遗漏），60px 窄栏里 label+箭头溢出。修复：移动端块补 `.console-brand{justify-content:center;padding:0 0 20px}`、`.console-menu-group-header{justify-content:center;padding:10px}`、隐藏分组头 span 与 chevron。另增强 index.html 设备检测：`update()` 函数在 load/resize/orientationchange 时按（移动UA || 触摸 || innerWidth<768）刷新 data-device，桌面窗口缩窄预览时也按移动端渲染。vue-tsc 通过、Vite 200。
- AdminConsole 分组头箭头移除：用户要求一级菜单（分组）后面不再显示向下箭头图标（PC 展开态与移动端都不要）。删除模板中分组头的 `lucide:chevron-down` AppIcon（及其 rotated class 绑定）、相关 CSS（`.lucide-chevron-down` margin-left:auto/rotate 过渡、collapsed 与 mobile 块里隐藏 chevron 的选择器），只保留分组头 icon+label。折叠图钉的 chevron-left/right 不受影响。vue-tsc 通过、Vite 200。
- AdminConsole 分组头箭头恢复（条件展示）：用户改为要求 PC 端「展开」菜单面板时显示该箭头图标。重新加回分组头 `lucide:chevron-down`（margin-left:auto 右对齐、expanded 时 rotated 旋转 180°），并在 `[data-device="desktop"] .is-collapsed`（PC 收起态）与 `[data-device="mobile"]`（移动端）两处隐藏。即：仅 PC 展开态显示。vue-tsc 通过、Vite 200。
- AdminHotWords.vue 工具栏精简：用户嫌统计周期/显示条数组件太大。改为：把 `.hw-scope` 提示（hotWordsScope「全平台全局统计，仅展示、不写回热词表」）从独立 <p> 移入 `.hw-toolbar` 第一项（左侧），控件包进 `.hw-fields`（margin-left 由 space-between 推到右侧、居右）；整体 `.hw-toolbar` 改 justify-content:space-between + flex-wrap。缩小组件：`.hw-seg` padding 4→3px、track top/bottom/left 4→3px、width calc((100%-8px) → (100%-6px))/count、`.hw-seg-btn` min-width 52→34px / height 26→20px / padding 0 12px→0 8px / font-size 12→11px、`.hw-label` 13→12px、`.hw-field` gap 10→8px。vue-tsc 通过、Vite 200。
- AdminHotWords.vue 条形图不随窗口缩放 bug 修复：根因是 `onBeforeUnmount` 里 `window.removeEventListener('resize', onResize)` 存在但 `onMounted` 从未注册过该监听（onMounted(load) 直接只调 load），导致缩放窗口/侧边栏收起时图表不重绘、条形超出容器或过小。修复：onMounted 里注册 window resize 监听 + 新增 `rootRef`（根容器）ResizeObserver（窗口缩放与侧边栏收起/展开都会改变图表宽度，ResizeObserver 兜底），onBeforeUnmount 对称移除监听并 disconnect。vue-tsc 通过、Vite 200。
- 全站图表 resize 排查（用户要求查概览+用量，热点图不管）：AdminOverview.vue 与 AdminUsage.vue 的 window resize 监听均**正确注册**（无 AdminHotWords 那种未注册 bug），浏览器缩放没问题；但都缺容器 ResizeObserver——侧边栏收起/展开会改变内容区宽度却不触发 window resize，图表不会自适应。修复：两页均新增 `rootRef` + `pageObserver`（ResizeObserver），Overview 的回调只 `charts.forEach(resize)`（词云由既有 wcObserver 自理，避免重复重排），Usage 回调直接 onResize；onBeforeUnmount 对称 disconnect。热点页按用户要求未改动。vue-tsc 通过、Vite 200。
- resize 性能优化（用户反馈概览页仍卡）：window resize 与容器 ResizeObserver 在窗口缩放时会同时触发造成同帧重复计算；词云 d3 布局在每次 resize 事件里同步重排，拖拽窗口时逐帧卡顿。新建 `src/utils/resize.ts`：`createRafCoalescer()`（rAF 合并，同帧至多一次）与 `createDebounced(fn, delay=120)`（高频后延迟执行一次）。改造 AdminOverview/AdminUsage/AdminHotWords 三页：window 监听与 RO 回调都走 `scheduleLayout(onResize)`（命名 handler onWindowResize 以便 removeEventListener 对称移除）；Overview 的 `onResize` 改为 `charts.forEach(resize) + scheduleCloud()`，`scheduleCloud = createDebounced(renderCloud,120)`，wcObserver 也改 `scheduleCloud`（词云等缩放稳定后再重排）。卸载后 pending 回调由 charts=[] 与 refs 置空天然兜底。vue-tsc 通过、Vite 全 200。
- **重要教训（@iconify/vue class）**：`@iconify/vue` 的 `<Icon>` 渲染的 svg 只带 `iconify iconify--{prefix}` 两个 class，**不会**生成 `lucide-chevron-down` 这类"图标名 class"。因此所有形如 `.lucide-chevron-down`、`.lucide-chevron-left` 的 CSS 选择器永远匹配不上（本会话中分组头箭头右对齐/旋转/收起态隐藏一直未生效即此因，用户反馈收起态箭头仍显示）。正确做法：给 `<AppIcon class="group-chevron">`（父级 class 会透传合并到 svg 上），CSS 用 `.group-chevron` / `.pin-chev` 定位。AdminConsole.vue 已把分组箭头与图钉箭头的选择器全部改为显式 class。vue-tsc 通过、Vite 200。
- 侧栏收起/展开文字跳动修复（AdminConsole.vue）：用户反馈展开瞬间左侧文字"往上串出来"。根因是收起态 label 用 `display:none` 瞬时切换——展开时文字立即出现且窄栏内换行溢出导致高度跳动。改为平滑揭示：`.console-brand span / .console-menu-item span / .console-menu-group-header span` 统一 `white-space:nowrap; overflow:hidden; max-width:200px; opacity:1; transition:max-width .2s, opacity .2s`；收起态由 `display:none` 改为 `max-width:0; opacity:0`（零宽度+透明，图标仍居中），同时收起态补 `gap:0`（否则 0 宽 label 仍占 flex gap 会把居中图标偏移 ~5px）。分组头 chevron 收起态保持 display:none。移动端仍用 display:none（无动画）。vue-tsc 通过、Vite 200。
- 二级菜单选中高亮修复（AdminConsole.vue）：用户要求选中的二级菜单高亮。根因：子菜单 `:class="{active: isActive(child.key)}"` 用 key 匹配，但 key（settings-base）≠ 路由路径（/admin/settings/base），高亮从未命中。修复：新增 `isChildActive(path)`（按完整路径匹配，含尾斜杠），内联子菜单（.console-menu-child）与收起态/移动端气泡（.console-flyout-item）的 active 绑定都改用它；高亮样式（primary 色 + glow 渐变背景）本就存在。vue-tsc 通过、Vite 200。
- 删除「热词配置」功能 + 聊天页推荐词改为高频词 TOP4（前后端）：
  - 后端（Desktop/ai-chat-server）：models.py 删公开 `/api/suggestions`，新增公开 `/api/hot-words?limit=N`（复用 hotwords.hot_words(period="month") 的缓存逻辑，无需鉴权）；admin.py 删 /suggestions CRUD 4 个路由 + SuggestionPayload 导入；schemas.py 删 SuggestionPayload；db.py 删 suggestions 建表 DDL 与 _seed_suggestions 及 init_db 调用（旧库遗留孤儿表无害，无代码引用）。
  - 前端：删除 AdminSuggestions.vue；router 删 suggestions 路由；AdminConsole 删 suggestions 菜单项与 TabKey；adminService 删 4 个 suggestion 函数；types/admin 删 SuggestionItem/SuggestionPayload；apiService 的 ChatSuggestion→ChatHotWord、fetchSuggestions→fetchChatHotWords(limit=4)（GET /hot-words?limit=4）；MessageList 推荐词改为 `hotWords = fetchChatHotWords(4) → word 数组`，suggestions computed 取前 4，**不再按 locale 取 title_zh/title_en**（语言切换不影响），空数据时 i18n `chat.suggestions` 兜底；删除 MessageList 中 i18n 导入（不再用）。
  - 验证：vue-tsc 通过、Vite 全 200；后端 py_compile OK；实测 GET /api/hot-words?limit=4 200 返回 top4（介绍/量子/计算/原理）、旧 /api/suggestions 与 /api/admin/suggestions 均 404、admin /api/admin/hot-words 带 token 200。
- **联网搜索全面检修 + 引用展示（研究驱动，用户确认走免费方案）**：
  - 研究发现的 6 个硬伤：①websearch.py 缺 `import json` → `_get_settings` 里 json.loads NameError 被外层 except 吞掉 → 所有后台搜索配置全部失效；②`_get_settings` 只查 `LIKE 'websearch_%'`，读不到 `searxng_url/searxng_timeout`；③`max_results` 死代码（`results[:max(1,MAX_RESULTS)]` 用常量）；④`_get_providers()` 从未被调用，供应商开关/优先级无效；⑤websearch 默认配置只在手动 seed 时写库，init_db 不写；⑥chat.py 搜索在 handler 里同步执行，响应头被 8-10s 搜索拖住 + 失败静默。
  - 修复（websearch.py）：补 import json；SQL 改 `(key LIKE 'websearch_%' OR key LIKE 'searxng_%')`；新增 `_get_max_results/_get_max_content`；`_get_providers` 归一化（兼容字符串/对象两种格式，顺序即优先级）；三源函数加 limit；`web_search` 用 gather 按供应商并行（**坑：Python 3.13 的 asyncio.as_completed 产出 `_wait_for_one` 协程，用 dict[task]=source 反查会 KeyError，必须 gather+zip(coros,results)**）；PageFetcher(max_length=_get_max_content())；`_dedupe_and_score` 加 provider_order 权重。
  - db.py：`_seed_settings` 增 7 个 websearch 键（providers 对象数组）；messages 加 `citations` 列（SCHEMA + `_migrate` PRAGMA 补列）。seed.py DEFAULT_SETTINGS 对齐。
  - chat.py：搜索移入 gen()（响应头秒开）；`_build_search_query` 清理指令前缀/剥 markdown 链接、过短回退会话首条用户消息；`_build_search_context` 返回 (messages, meta)，token 预算（总 5000 字符、单条正文 1200、citations≤6）；SSE 下发 `{"search":{status:started/done/no_results/failed, query, count, duration_ms, sources, error, citations}}`；`_persist_assistant` 增 citations 参数写库；sessions.py get_messages 返回 citations。
  - 前端：types/chat 新增 Citation/SearchStatus + Message.citations/searchStatus；apiService parseStreamLine 识别顶层 search + onSearch 回调 + search 事件计为流活动（防 30s 首包误杀）；chatStore 删硬编码"🔍 搜索耗时 Xs"前缀、onSearch 写 searchStatus/citations；MessageItem 加搜索状态徽标（done/no_results/failed）与可点击"来源"链接列表（顺带修复 `.message-searching` 样式误嵌在 @media 里的 bug）；locales 加 chat.searching/searchDone/searchFailed/searchNoResults/sources；AdminSettingsSearch load() 补读 websearch_max_results/websearch_max_content + providers 字符串→对象归一化。
  - 验证：py_compile OK；实测 chat web_search=true SSE 首部 started→done（count=6、12.8s、sources=[bing]、6 citations，SearXNG/DDG 失败自动剔除）；后台 PATCH max_results=3 后 count=3（配置真生效）；web_search=false 无 search 事件；带 session_id 落库后 get_messages 返回 citations（2 条）；前端 vue-tsc + vite build 通过。
- **搜索中文相关性修复（用户反馈搜"美股如何开户"返回美-百度百科/美团/美的等垃圾）**：
  - 根因：①**Bing RSS 对中文长查询只按首字/片段匹配**（实测 q=美股如何开户 返回"美"字结果，加 mkt/引号/cn.bing.com 均无效）；②Bing HTML 被墙（len=214）、DDG 被风控（len=0）、公共 SearXNG 不可达——中文环境实际只剩 Bing RSS 且返回垃圾；③`_dedupe_and_score` 用 `re.findall(r"[\w\u4e00-\u9fff]+")` 把整句中文当一个 token，关键词重合打分对中文永远为 0，无法把相关结果排上来。
  - 修复：①新增**百度源** `_baidu_search`（GET baidu.com/s?wd=，跳过 baidu.php/ada.baidu 广告与相对链接，只取 baidu.com/link 有机结果；摘要取 h3 到下一个 h3 之间的容器可见文本清洗截断 160 字符——实测干净可用）；②`_dedupe_and_score` 改用 **jieba 分词** `_tokens()`（标题×3+摘要×1+来源优先级），中文关键词真正命中；③百度加入默认供应商列表且**置顶**（db.py `_seed_settings`、seed.py DEFAULT_SETTINGS、AdminSettingsSearch providerOptions 三处同步；线上库已直接 UPDATE providers 为 [baidu,searxng,bing,ddg]）。
  - 验证：web_search("美股如何开户") 实测 6 条全为百度相关结果（叩富网开户条件/开户教程/入门指南），摘要干净；chat SSE done 事件 sources=['baidu']、citations 全相关；前端 vue-tsc + Vite 200。注意：Bing RSS 中文失效、DDG 国内网络被风控是环境事实，百度为中文主源，SearXNG/Bing/DDG 作为其他语言/兜底。
- **DeepSeek 思考流超时修复 + 思考过程展示（用户反馈 V4-Flash + 联网搜索报"流式传输空闲超时"、1M+ 数据但一个字不渲染）**：
  - 根因：DeepSeek V4-Flash 等推理模型先流式吐 `delta.reasoning_content`（思考过程），`content` 为 null；前端 `parseStreamLine` 只读 `delta.content` → 思考分片返回 null、**不计入"流活动"** → 60s 空闲计时器不重置，思考一长就被掐断，且 1M+ 数据全是没被渲染的思考内容。
  - 修复：①apiService `parseStreamLine` 识别 `reasoning_content` 并返回 `{content:'', reasoning}`，有 delta 但无内容（如仅 role 分片）也返回有效；②流循环重构：**任何有效分片（content/reasoning/search/meta/role）都清首包计时并重置空闲计时器**；③`ChatStreamOptions` 新增 `onReasoning`；④`MAX_TOTAL_TIMEOUT` 5min→10min（长思考+长回答兜底）；⑤types/chat `Message.reasoning?`；chatStore `onReasoning` 拼接进 msg.reasoning；MessageItem 新增可折叠"💭 思考过程"块（默认折叠、点击展开、max-height 240 滚动）；locales 加 chat.reasoning（思考过程/Reasoning）。思考内容仅当前会话内存展示，不落库（与 DeepSeek 官方一致）。
  - 验证：Node 脚本按前端同款 parse+空闲逻辑模拟真实 V4-Flash+联网搜索流：总耗时 31.4s、search started→done、reasoning 84B、content 658B、**分片最大间隔 17.5s < 60s**、流完整结束 → 不会超时、正文正常渲染；vue-tsc + Vite 全 200。
- **"一直显示联网搜索中直到页面崩溃"严重 bug 修复（chatStore/apiService/chat.py）**：
  - 根因1（卡在搜索中）：`sendMessageStream` 的 catch 里 `AbortError` 直接 return、finally 只清 loading **没清 isSearching**——只要流被中断（搜索阶段无事件 >60s 空闲超时 abort；或后端 gen() 里非 httpx 异常导致流静默中断、无 done 事件但流正常结束），`isSearching` 永远 true → 一直"联网搜索中"。
  - 根因2（页面崩溃/卡死）：思考内容 `msg.reasoning = (msg.reasoning||'') + text` 无限累加，超长思考（1M+）是 O(n²) 字符串操作，直接拖垮页面。
  - 修复：①chatStore `finally` **无条件清除 isSearching/searchingText**（成功/失败/中断都恢复）；②onReasoning 思考内容**封顶 12K 字符**（slice(-12000)，防 O(n²)）；③apiService `IDLE_TIMEOUT` 60s→90s（搜索阶段多源+正文抓取最坏约 50s，留余量防刚搜完就被掐）；④后端 chat.py gen()：搜索阶段包 try/except，异常时下发 `search failed` 事件**绝不让流静默中断**；gen() 的 except 从 `httpx.HTTPError` 拓宽为 `except Exception`（下发 error 事件）并补 logger。updateMessage 确认是原地修改（引用不失效，onSearch/onReasoning 捕获的 msg 有效）。
  - 验证：后端 py_compile OK、前端 vue-tsc OK、回归模拟（V4-Flash+联网搜索）started→done、reasoning 137B、content 387B、最大间隔 17.8s、流完整结束。
- **"发消息页面就崩溃"死循环 bug（apiService.ts，上一轮引入的回归）**：
  - 根因：流解析重构时加了 `if (!parsed) continue`，但 SSE 流中大量空行分隔（`\n\n`）→ `parseStreamLine('')` 返回 null → `continue` **跳过循环末尾的 `newlineIndex = buffer.indexOf('\n')` 推进** → 内层 while 用同一个 index 反复处理同一行 → 无限循环 → 页面卡死/崩溃。与是否联网搜索无关（任何消息都会触发）。注：当时 Node 模拟脚本里 `continue` 前先更新了 nl，所以脚本没暴露、真代码炸了。
  - 修复：处理器改为包进 `if (parsed) { ... }`，`newlineIndex = buffer.indexOf('\n')` **无条件执行**（无论该行是否可解析都推进）。
  - 验证：复刻修复后循环喂真实流（V4-Flash 不联网）：50 分片、思考 111 字符、正文 42 字符、2.1s 完成、无死循环；vue-tsc + Vite 200。**教训：SSE 解析的内层 while 里永远不要用不带 index 推进的 continue。**
- **会话流式渲染性能优化（用户反馈发消息卡顿/崩溃，要求着重优化会话性能）**：
  - 三个瓶颈：①MessageItem `renderedContent` 每个 token 都 `marked.parse`+DOMPurify+`v-html` 整块替换（O(n²)）；②chatStore `updateMessage` 每个 token 都写 `session.lastPreview/updatedAt` 触发侧栏 `sortedSessions`/`filteredSessions` 整块重渲染；③MessageList 对 `props.messages` 用 `{deep:true}` watch，每个 token 深遍历整个数组+scrollTo。
  - 修复：①MessageItem 流式期间（`message.loading`）改渲染纯文本 `.streaming-text`（pre-wrap），结束后一次性 `v-html` 渲染 Markdown；②chatStore 新增 `updateMessageContent`（只改 message.content、不碰会话 meta），onChunk 改为本地 `accContent` 累积 + `requestAnimationFrame` 合帧 flush（最多 ~60/s），流结束取消 rAF 并做最终 flush；③MessageList 拆成两个浅 watch：`messages.length`（新消息/前插判断）+ 最后一条 `content.length`（流式增长跟随滚动），删除 `deep:true`。
  - 验证：无头 Chrome（puppeteer-core + 系统 Chrome）真实复现：发消息 60s 全程响应、0 错误、不冻结；"1+1等于几" 正常返回"1+1等于2。"且流结束后 Markdown 正确渲染（.markdown-body 出现、.streaming-text 消失）。vue-tsc + Vite 全 200。
- 新增会话跳转修复（chatStore.createNewSession）：用户要求点"新增会话"直接跳转到新会话。根因：成功路径 `applyList(await apiService.createSession())` 只刷新列表——`applyList` 仅在当前会话不在新列表时才切换（新会话是追加、旧会话仍在，故不切），只有离线兜底路径设置了 currentSessionId。修复：成功路径取返回值 `list[0]`（后端 create_session 返回 `_session_rows` 完整列表、最新在前）显式 `currentSessionId.value = created.id`。验证：无头 Chrome 先切到第 2 个会话（activeIdx=1、2 条消息）再点"新建会话"→ 跳到列表第一项（activeIdx=0、空态、0 消息）✓。vue-tsc + Vite 200。
- **模型联网搜索能力位 supports_search（用户确认"做吧"，全链路实现）**：
  - 背景：小模型经常不读取搜索上下文（见前面 Q2 分析），彻底方案是按模型加能力位。后端：db.py models 表加 `supports_search INTEGER NOT NULL DEFAULT 1`（SCHEMA + `_migrate` 补列）；schemas ModelPayload 加 `supports_search: bool = True`；admin.py create/update_model 增删该列；models.py 公开 /api/models 返回 supports_search；chat.py gen() `if body.web_search and not model.get("supports_search", 1)` → 跳过搜索并下发 `{"search":{"status":"unsupported","error":"该模型不支持联网搜索","citations":[]}}` 事件。
  - 前端：types/chat ModelInfo.supportsSearch + SearchStatus 增加 'unsupported'；types/admin AdminModel/ModelPayload 加 supports_search；InputBox `searchSupported = currentModelInfo?.supportsSearch !== false`（缺省视为支持），不支持时按钮 disabled + tooltip（input.searchUnsupported）；chatStore.setModel 切到不支持模型时自动 PATCH web_search=false；ModelManage 加"联网"列（check-circle 图标）与表单勾选（emptyForm/toPayload 同步）；MessageItem 处理 unsupported 状态显示；i18n zh/en：console.supportsSearch='联网'/'Web Search'、input.searchUnsupported、chat.searchUnsupported。
  - 验证：py_compile OK、迁移后 models 表含列、公开接口返回 supports_search；实测把 GLM-4-9B 标记不支持 → chat web_search=true 返回 `unsupported` 事件（已恢复）；前端 vue-tsc + Vite 全 200、无头 Chrome 模型管理页显示"联网"列 ✓。
  - 验证：py_compile OK；实测 chat web_search=true SSE 首部 started→done（count=6、12.8s、sources=[bing]、6 citations，SearXNG/DDG 失败自动剔除）；后台 PATCH max_results=3 后 count=3（配置真生效）；web_search=false 无 search 事件；带 session_id 落库后 get_messages 返回 citations（2 条）；前端 vue-tsc + vite build 通过。
- 概览页近30天趋势图 y 轴 K/W/M 格式化，与用量页统一：概览原 `fmtTokens` 只有 K/M 两档（无 W），已升级为与 AdminUsage `formatTokens` 同构（≥1e8→X亿/≥1e6→XM/≥1e4→XW/≥1e3→XK，参数改 number|string）；趋势图双 y 轴 axisLabel + tooltip valueFormatter 套用 fmtTokens，grid.left 46→64、right 50→60 容纳 W/M。顺带按模型柱状图 x 轴也享同一格式化。vue-tsc 通过。

- 收尾「高频热词」功能（上次被 429 打断）：确认后端代码到位（hotwords.py / db.py 索引 / main.py include / jieba 0.42.1 已装）。启动后端 `nohup .venv/bin/uvicorn ai_chat_server.main:app --reload --host 127.0.0.1 --port 8000`（日志 /tmp/ai-chat-server.log，已验证 Application startup complete）。自测全绿：登录 admin 拿 token → 无 token 调用 /api/admin/hot-words 返回 401；带 token 返回 200 + 真实热词（介绍/量子/计算/原理…）；非法 period 返回 400。前端 `npm run type-check`（vue-tsc --build）零报错；经 Vite 代理 5173→8000 的端到端调用返回 200 真实数据。功能已可用。

- 概览页（AdminOverview.vue）调整：删除「按性别分布」环形图（模板段 + renderDonut(genderRef) 调用 + genderRef/genderLabel）与「最新注册用户」列表（模板段 + fmtTime/fmtRegion 仅此处使用，一并删）。在 ov-grid--3 末尾新增「高频词云」面板（ov-wordcloud-panel，grid-column: span 2，占两张卡片宽度，置于最后）。词云为纯 CSS flex 标签云（不引入 echarts-wordcloud，因 echarts 已升到 v6 且 wordcloud 仅兼容 v5、且会与本文件 echarts/core 按需引入冲突）。数据走既有 fetchHotWords('month',50)，独立于 fetchOverview 获取（失败不挡概览）。字号按词频平方根映射到 15~55px；配色刻意避开品牌青(#00e5ff)/紫(#7c5cff)，用 9 色柔和亮色调（teal/blue/amber/rose/emerald/pink/sky/orange/lime）防撞色；hover scale(1.16)+currentColor 辉光。i18n 新增 console.ovHotWords / ovHotWordsHint / ovHotWordsEmpty（zh+en）。vue-tsc 通过，Vite 编译 200。

- 修正：概览页「高频词云」由纯 CSS flex 标签云改为 **d3-cloud 径向词云**（npm i d3-cloud + @types/d3-cloud）。原因：用户要求“最大词在中间、由内向外越来越小”，flex 布局无法做到；d3-cloud 按传入顺序从中心螺旋外扩且大词优先，故把词按词频降序传入即可保证中心最大、越外越小。渲染为绝对定位 span（left/top = 容器中心 + d3 坐标，transform translate(-50%,-50%)），保留既有着色（避开品牌青/紫的 9 色柔色调）与 hover 辉光；容器高度 300px（移动端 240px）。用 ResizeObserver + window resize 重算布局，token 守卫丢弃过期布局防闪烁；容器挂载（wcLoading=false 后 nextTick）才 setupCloudObserver/renderCloud，修复了首次 onMounted 时容器尚未挂载导致不渲染的时序问题。vue-tsc 通过。

- 概览页高频词云面板（AdminOverview.vue）新增「统计周期 / 显示条数」控制条，借鉴热点页面（AdminHotWords）的交互但升级为科技风+液态玻璃风：周期用 HEAT_PERIODS（日/周/月/年）分段药丸，条数用 10/20/50 等宽数字分段（替代原先简陋的 <select>）。玻璃药丸容器 rgba 半透明 + inset 高光 + blur(10px) + 投影；激活态青→紫霓虹渐变 + glow。切换周期/条数走 loadHotWords()：首次 loading、其后仅右上角轻量 busy 遮罩（wcBusy），避免容器卸载导致 ResizeObserver 失效；setupCloudObserver 改为先 disconnect 旧观察器再挂新（容器在有无数据间会重建）。提示文案 ovHotWordsHint 改为动态插值 {period}（zh/en 均已改）。vue-tsc 通过、Vite 编译 200；后端 hot-words 各 period×limit 组合 curl 实测均 200 返回真实数据。

- 修正：概览页词云「统计周期/显示条数」选择器样式改为借鉴热点地图（AdminMap.vue）右上角周期组件——液态玻璃药丸（var(--color-glass) + blur(var(--glass-blur)) + inset var(--color-glow) 辉光）+ 底部青→紫渐变滑动指示条（.ov-seg-track，translateX(index*100%)，transition cubic-bezier 0.38s，宽度 calc((100%-8px)/var(--seg-count))），按钮等宽 mono、激活态白字+辉光，完全跟随主题变量。新增 wcPeriodIndex/wcLimitIndex computed 驱动轨道。替换掉上一版 rgba 半透明自绘药丸。

- 重要澄清：用户所指「高频词分析」= AdminHotWords.vue 页面（菜单：高频词分析，工具栏有 统计周期/显示条数），而非概览页词云面板。前两轮我把样式改到了 AdminOverview.vue（概览词云也新增了同类选择器，保留、无害），导致用户反馈「样式还是没有生效」。本轮把 AdminHotWords.vue 工具栏改造成热点地图同款：周期(4段)+条数(10/20/50, 3段)均为液态玻璃药丸+青紫渐变滑动条（.hw-seg/.hw-seg-track/.hw-seg-btn，translateX(index*100%)），删除原生 <select>（HW_LIMITS 常量，switchLimit(n) 带参），periodIndex/limitIndex computed 驱动轨道。vue-tsc 通过、Vite 编译 200。

- 概览页词云面板微调：去掉「显示条数」控制（删除 wcLimit/WC_LIMITS/wcLimitIndex/switchWcLimit，loadHotWords 固定取 50）；保留「统计周期」药丸但去掉「统计周期」标签文字，工具栏 justify-content: flex-end 居右；右上角提示文案 zh 改为「近一{period}用户提问高频词」（日/周/月/年前加"一"，en 不变）。清理 .ov-wc-field/.ov-wc-label 无用样式。vue-tsc 通过、Vite 200。

- 概览页词云面板再调整：① 去掉「年」周期选项——新增 `WC_PERIODS = HEAT_PERIODS.filter(p=>p.key!=='year')`（仅日/周/月），模板 v-for 改 WC_PERIODS、--seg-count 4→3，periodLabel/wcPeriodIndex 同步改 WC_PERIODS（热点地图页不受影响，仍保留年）。② 缩小尺寸更优雅：词云画布 height 300→210px（移动端 240→180）、wrap/loading min-height 260/240→190/180；云字号上限 54→40px（cloudSize 14+t*26）；工具栏 margin-bottom 16→12、gap 收紧。vue-tsc 通过、Vite 200。

- 概览页词云面板继续缩小：确认 `.ov-*` 词云样式为 `<style scoped>` 且仅存在于 AdminOverview.vue（地图页用 .map-period、热点页用 .hw-seg，均不共享），故可安全缩。本轮将面板从占 2 列改为 `grid-column: span 1`（宽度减半），画布 height 210→168px（移动端 180→150）、wrap/loading min-height 190/180→150；云字号上限 40→36px（cloudSize 14+t*22）。vue-tsc 通过、Vite 200。

- 重要纠正：用户说"缩小这个组件"指的是**右上角日/周/月切换器（.ov-seg 药丸）**，不是词云图！之前两轮把词云面板从 span2/300px 缩到 span1/168px 是误读。已回退词云到原始尺寸（grid-column: span 2、画布 height 300px/min 260px、移动端 240/220、云字号上限恢复 54px=14+t*40），并**仅缩小切换器**：.ov-seg padding 4→3px、.ov-seg-track 内缩 4→3px、.ov-seg-btn min-width 52→38px / height 26→22px / padding 0 12→0 10 / font 12→11px / letter-spacing 0.06→0.03em。年选项仍去掉（WC_PERIODS）。vue-tsc 通过、Vite 200。教训：用户口中的"组件/这个组件"在此语境指工具栏里的切换器，缩放宽高应针对该控件而非整块词云。

- 概览页词云面板：把右上角日/周/月切换器（.ov-seg 药丸）从独立工具栏行（.ov-wc-toolbar，已删除）移入标题行 h3.ov-panel-title，紧跟提示文案"近一月提问高频词"之后，与之和标题右边缘同行对齐（标题 flex + 提示 margin-left:auto 推到右侧，再加 .ov-seg--inline{margin-left:6px} 与提示留白）。删除 .ov-wc-toolbar 及其移动端覆盖。词云尺寸保持原始（span2/300px/字号54）。vue-tsc 通过、Vite 200。

- 概览页词云面板：应要求删除标题行提示文案"近一{period}用户提问高频词"（模板 .ov-wc-hint span、样式 .ov-wc-hint、已不再使用的 periodLabel computed、locale 键 ovHotWordsHint 的 zh/en 共 4 处一并清理）。切换器改用 `.ov-seg--inline{margin-left:auto}` 自行右对齐到标题行最右，不再依赖提示文字占位。vue-tsc 通过。

- 主题切换自适应：概览页（AdminOverview.vue）与用量管理页（AdminUsage.vue）的 ECharts 图表原本只在 onMounted 画一次、颜色取自 CSS 变量（--color-primary/--color-accent/--color-text-secondary/--color-border），切主题只改 CSS 变量不触发重绘，颜色停留旧主题。修复：两页均引入 useChatStore 并新增 `watch(() => chatStore.currentTheme, () => { if (overview/usage) renderAll()/render() })`（与语言切换 watch 同理；时序上 selectTheme 先 setTheme 再同步 applyTheme，watch 回调在 microtask 中执行时 CSS 变量已更新，cssVar() 能读到新值）。另把 setup 里一次性求值的 `pieColors` 常量改为 `pieColors()` 函数（每次 render 重新读主题色，前两项 primary/accent 跟随主题，其余中性色不变），Overview 的 renderDonut 与 Usage 的 age/gender 两处 `color:` 改 `pieColors()`。vue-tsc 通过，Vite 编译 index/overview/usage 均 200。备注：词云颜色为刻意避开主题色的硬编码柔和色，未纳入本次自适应；热点地图 AdminMap 未在本次需求范围内。

- 高频词分析页（AdminHotWords.vue）条形图同样做主题自适应：引入 useChatStore，新增 `watch(() => chatStore.currentTheme, () => { if (words.value.length) render() })`（该页 render 里 axisColors() 本就是函数、series 用 c.primary、label 用 c.text，重绘即套用新主题色，无需改 pieColors——此页无饼图）。vue-tsc 通过、Vite 编译 200。

- 管理控制台（AdminConsole.vue）加侧边栏收起/展开（仅 PC 端）：侧边栏底部新增折叠按钮 `.console-collapse-btn`（切换 `collapsed` ref，localStorage 'adminConsoleCollapsed' 持久化，刷新保留）；收起后侧边栏宽度 220→64px、隐藏所有文字/分组箭头/二级菜单，仅留图标，主内容区 flex:1 自动变宽。折叠逻辑全部包在 `@media (min-width: 769px)` 内，移动端（≤768px，原已只显示图标）完全不受影响；移动端还用 CSS 把折叠按钮 `.console-collapse-btn` 隐藏（`@media (max-width:768px)`）。收起态下点击「设置」分组会先自动展开侧边栏再展开二级（否则二级在 64px 内放不下）。`.console-nav` 加 `transition: width 0.25s ease` 平滑过渡。新增 i18n 键 console.collapse='收起'/expand='展开'（zh+en）。vue-tsc 通过、Vite 编译 200。
